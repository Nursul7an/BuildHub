{
  "name": "build-hub",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@build-hub/server & npm run dev --workspace=@build-hub/web",
    "dev:web": "npm run dev --workspace=@build-hub/web",
    "dev:server": "npm run dev --workspace=@build-hub/server",
    "build": "npm run build --workspace=@build-hub/shared && npm run build --workspace=@build-hub/server && npm run build --workspace=@build-hub/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:reset": "npm run db:reset --workspace=@build-hub/server",
    "seed": "npm run seed --workspace=@build-hub/server",
    "verify": "npm run seed && node scripts/sweep.mjs && npm run seed && node scripts/flow.mjs && npm run seed && node scripts/actions.mjs",
    "verify:actions": "npm run seed && node scripts/actions.mjs",
    "verify:screens": "node scripts/sweep.mjs",
    "verify:flow": "npm run seed && node scripts/flow.mjs",
    "shots": "node scripts/screens.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
